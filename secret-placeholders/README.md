# Secret Placeholders

This directory stores only non-sensitive placeholder metadata for AWS Secrets Manager entries used by the API proxy.

Do not commit real secret values, tokens, client secrets, private keys, signed URLs, or copied AWS console secret payloads here.
